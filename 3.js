let puntaje = [

];

for (let index = 0; index < puntaje.length; index++) {
    const element = puntaje[index];
    let totalJugador = 0;
    for (let index = 0; index < puntaje.length; index++) {
        const element = puntaje[index];
        let totalJugador += puntaje [i][j]; 
        
    }
     console.log ('jugador ${i + 1}) - Total: ${totalJugador} pts ');

}